# 预测模型技术详解报告

> 面向对象：技术人员（算法工程师、数据科学家、机器学习工程师）
>
> 主题：从「表格数据」与「时序数据」两条主线，系统梳理预测模型（回归 / 分类）的技术演进、典型模型与核心原理。
>
> 阅读建议：本报告既可作为 PPT 讲稿的底稿，也可作为技术速查手册。每一节都包含「技术原理 → 数学/结构 → 工程要点 → 适用场景」四个维度。配图位置已用 `🖼️ 图片占位` 标注，并附上推荐配图说明与论文/资料链接，方便自行截图或下载。

---

## 目录

1. [总览：预测任务的分类与技术地图](#1-总览预测任务的分类与技术地图)
2. [表格数据预测（Tabular Regression & Classification）](#2-表格数据预测tabular-regression--classification)
   - 2.1 [线性模型时代](#21-线性模型时代线性回归--逻辑回归)
   - 2.2 [核方法与 SVM](#22-核方法与-svm)
   - 2.3 [决策树与集成学习](#23-决策树与集成学习随机森林--gbdt)
   - 2.4 [梯度提升三巨头：XGBoost / LightGBM / CatBoost](#24-梯度提升三巨头xgboost--lightgbm--catboost)
   - 2.5 [表格深度学习与表格基础模型（含 TabPFN 深度解读）](#25-表格深度学习与表格基础模型tabnet--ft-transformer--tabpfn)
   - 2.6 [表格预测技术选型小结](#26-表格预测技术选型小结)
3. [时序数据预测（Time-Series Regression & Classification）](#3-时序数据预测time-series-regression--classification)
   - 3.1 [经典统计模型](#31-经典统计模型ar-arima-ets)
   - 3.2 [机器学习方法](#32-机器学习方法特征工程--gbdt)
   - 3.3 [RNN / LSTM / GRU（含 LSTM 深度解读）](#33-rnn--lstm--gru含-lstm-深度解读)
   - 3.4 [CNN 与时序卷积 TCN](#34-cnn-与时序卷积-tcn)
   - 3.5 [Transformer 时序家族](#35-transformer-时序家族informer--autoformer--fedformer--patchtst)
   - 3.6 [线性模型的反击与时序基础模型（含 Chronos 深度解读）](#36-线性模型的反击与时序基础模型dlinear--n-beats--timesfm--chronos)
   - 3.7 [时序分类专题](#37-时序分类专题)
4. [横向对比与工程实践建议](#4-横向对比与工程实践建议)
5. [参考资料与配图清单](#5-参考资料与配图清单)
6. [附录 A：可参考的同类开源项目推荐](#附录-a-可参考的同类开源项目推荐)

---

## 1. 总览：预测任务的分类与技术地图

预测建模本质是学习一个映射 `f: X → y`。按照 **输入数据结构** 与 **输出类型** 可以划分为四个象限：

| 数据类型 ↓ \ 输出 → | 回归（连续值） | 分类（离散标签） |
| --- | --- | --- |
| **表格数据**（行=样本，列=特征，样本间独立） | 房价预测、销量预测、CTR 预估的概率 | 风控违约判定、用户流失、欺诈检测 |
| **时序数据**（按时间排序，样本间有时间依赖） | 负荷预测、股价预测、需求量预测 | 心电图诊断、设备故障检测、行为识别 |

**核心区别**：
- 表格数据假设样本 **独立同分布（i.i.d.）**，关注「特征之间的交互」。
- 时序数据存在 **时间依赖、趋势、季节性、自相关**，关注「历史如何影响未来」。

> 🖼️ **图片占位 1：预测任务技术地图（四象限 + 技术演进时间轴）**
> 推荐做法：自制一张二维矩阵图，横轴为「回归/分类」，纵轴为「表格/时序」，并叠加一条从 1960s（线性/ARIMA）→ 2000s（集成学习）→ 2017+（Transformer）→ 2023+（基础模型）的时间轴。
> 可参考的演进图素材：Papers with Code 趋势页 https://paperswithcode.com/area/time-series

---

## 2. 表格数据预测（Tabular Regression & Classification）

表格数据是工业界占比最高的数据形态。一条主线贯穿其技术演进：**从「人工设定假设的线性模型」走向「自动捕捉非线性与交互的树集成」，再到「试图用深度学习统一表征」**。

> 🖼️ **图片占位 2：表格模型技术演进树**
> 推荐：画一条从 线性回归 → SVM → 决策树 → 随机森林 → GBDT → XGBoost/LightGBM/CatBoost → TabNet/FT-Transformer 的演进树状图。

### 2.1 线性模型时代（线性回归 & 逻辑回归）

**技术原理**

- **线性回归（Linear Regression）** 假设目标是特征的线性组合：

  $$\hat{y} = w^\top x + b$$

  通过最小化均方误差（MSE）求解，闭式解为 $w = (X^\top X)^{-1} X^\top y$（最小二乘法）。

- **逻辑回归（Logistic Regression）** 在线性组合外套一层 Sigmoid，把输出压缩到 (0,1) 作为概率，用于分类：

  $$P(y=1\mid x) = \sigma(w^\top x + b),\quad \sigma(z)=\frac{1}{1+e^{-z}}$$

  损失函数为交叉熵（对数损失），用梯度下降求解。

**工程要点**
- 必须做 **特征标准化**；常配合 L1（Lasso，稀疏/特征选择）/ L2（Ridge，抗共线性）正则。
- 可解释性极强：权重即特征贡献，是金融风控评分卡的基础（WOE 编码 + 逻辑回归）。

**优缺点**
- ✅ 简单、快、可解释、不易过拟合；适合作为 baseline。
- ❌ 只能拟合线性关系，需人工构造交叉特征才能表达非线性。

> 🖼️ **图片占位 3：逻辑回归 Sigmoid 决策边界示意图**
> 资料：吴恩达机器学习课程经典插图，或 scikit-learn 官方示例 https://scikit-learn.org/stable/modules/linear_model.html

### 2.2 核方法与 SVM

**技术原理**

支持向量机（Support Vector Machine, SVM）的核心思想是 **最大间隔（max-margin）**：寻找一个超平面，使得两类样本到超平面的间隔最大。优化目标（软间隔）：

$$\min_{w,b}\ \frac{1}{2}\|w\|^2 + C\sum_i \xi_i \quad \text{s.t.}\ y_i(w^\top x_i + b)\ge 1-\xi_i$$

**核技巧（Kernel Trick）**：通过核函数 $K(x_i,x_j)=\phi(x_i)^\top\phi(x_j)$ 在高维空间隐式计算内积，无需显式映射，即可处理非线性。常用 RBF 核：

$$K(x_i,x_j)=\exp(-\gamma\|x_i-x_j\|^2)$$

- 回归版本称为 **SVR（Support Vector Regression）**，用 ε-不敏感损失。

**优缺点**
- ✅ 在中小样本、高维数据上效果好，理论优美（VC 维、结构风险最小化）。
- ❌ 大样本（>10万）训练慢（核矩阵 O(n²)），对参数 C、γ 敏感，难以解释。

> 🖼️ **图片占位 4：SVM 最大间隔 + 支持向量示意图**
> 资料：scikit-learn SVM 文档 https://scikit-learn.org/stable/modules/svm.html

### 2.3 决策树与集成学习（随机森林 & GBDT）

**决策树（Decision Tree）**

通过递归地按特征阈值切分样本空间，形成 if-else 规则树。分裂准则：
- 分类：信息增益 / 信息增益比（ID3、C4.5）、基尼不纯度（CART）。
- 回归：方差减少（最小化子节点 MSE）。

单棵树易过拟合 → 引出集成学习两大流派：

| 流派 | 代表 | 思想 | 降低 |
| --- | --- | --- | --- |
| **Bagging（并行）** | 随机森林 Random Forest | 自助采样 + 随机特征子集，训练多棵独立树后投票/平均 | 方差 Variance |
| **Boosting（串行）** | GBDT | 后一棵树拟合前面所有树的残差，逐步逼近 | 偏差 Bias |

**随机森林**：通过「样本扰动（Bootstrap）+ 特征扰动」降低树之间的相关性，集成后显著降方差，几乎无需调参，自带 OOB 估计与特征重要性。

**GBDT（Gradient Boosting Decision Tree）**：将提升过程视为在函数空间做梯度下降。第 m 棵树拟合损失函数关于当前预测的负梯度：

$$F_m(x) = F_{m-1}(x) + \nu \cdot h_m(x),\quad h_m \approx -\frac{\partial L}{\partial F_{m-1}}$$

其中 $\nu$ 是学习率（shrinkage）。

> 🖼️ **图片占位 5：Bagging vs Boosting 对比图 + GBDT 残差拟合过程图**
> 资料：StatQuest（Josh Starmer）Gradient Boost 系列图非常直观 https://www.youtube.com/c/joshstarmer ；或 ESL 书图。

### 2.4 梯度提升三巨头：XGBoost / LightGBM / CatBoost

这三者是 **表格数据预测的事实标准（SOTA baseline）**，Kaggle 结构化比赛的常胜将军。它们都是 GBDT 的工程化与算法化升级。

#### XGBoost（2016, 陈天奇）

- **二阶泰勒展开**：不仅用一阶梯度，还用二阶导数（Hessian）近似损失，收敛更快更准：

  $$\mathcal{L}^{(t)} \approx \sum_i \left[g_i f_t(x_i) + \tfrac{1}{2}h_i f_t^2(x_i)\right] + \Omega(f_t)$$

- **正则化项 $\Omega$**：显式惩罚叶子数量与叶子权重，控制复杂度。
- **工程优化**：列块并行、缓存感知、稀疏感知（自动处理缺失值）、近似分裂点查找。

#### LightGBM（2017, 微软）

针对大数据的速度优化：
- **Histogram 直方图算法**：把连续特征分桶（如 255 个 bin），分裂时只需遍历 bin，大幅降内存与计算。
- **Leaf-wise 生长**：不像 XGBoost 的 level-wise（按层），而是每次分裂增益最大的叶子，更快收敛但需 `max_depth` 防过拟合。
- **GOSS**（单边梯度采样）：保留大梯度样本，对小梯度样本采样。
- **EFB**（互斥特征捆绑）：把稀疏的互斥特征合并降维。

#### CatBoost（2017, Yandex）

针对类别特征与过拟合：
- **Ordered Target Encoding（有序目标编码）**：用「该样本之前的历史样本」计算目标统计，避免目标泄漏（target leakage）。
- **Ordered Boosting**：解决预测偏移（prediction shift），减少过拟合。
- **对称树（Oblivious Trees）**：每层用同一分裂条件，推理极快。

#### 三者横向对比

| 维度 | XGBoost | LightGBM | CatBoost |
| --- | --- | --- | --- |
| 训练速度 | 中 | **快** | 中 |
| 大数据扩展 | 良 | **优** | 良 |
| 类别特征 | 需手动编码 | 原生支持 | **原生 + 最优** |
| 默认精度 | 高 | 高 | **开箱即用高** |
| 过拟合控制 | 需调参 | 需调 num_leaves | **较稳健** |
| 小数据 | 良 | 易过拟合 | **优** |

> 🖼️ **图片占位 6：Level-wise vs Leaf-wise 树生长对比图 + 直方图分桶示意**
> 资料：LightGBM 官方文档 https://lightgbm.readthedocs.io/ ；XGBoost 论文 https://arxiv.org/abs/1603.02754 ；CatBoost 论文 https://arxiv.org/abs/1706.09516

### 2.5 表格深度学习与表格基础模型（TabNet / FT-Transformer / TabPFN）

深度学习在图像/文本上碾压传统方法，但在 **表格数据上长期打不过 GBDT**。原因：表格特征异构、无空间/序列局部性、样本量常较小。近年的尝试：

- **TabNet（Google, 2019）**：用「序列注意力（sequential attention）」在每个决策步动态选择特征，兼具可解释性与端到端学习能力。
- **FT-Transformer（2021）**：把每个特征（数值/类别）编码为 token，送入 Transformer 做特征交互，是目前表格深度学习中最强的 baseline 之一。
- **NODE（Neural Oblivious Decision Ensembles, 2019）**：用可微分的 oblivious 决策树，把 GBDT 思想神经网络化，支持端到端训练。
- **SAINT / TabTransformer**：引入行间注意力与对比预训练。

**现状结论（重要）**：多项基准研究（如 *“Tabular Data: Deep Learning is Not All You Need”*, Shwartz-Ziv & Armon 2021；以及 Grinsztajn 2022）表明，**在中小规模表格数据上，GBDT（XGBoost/LightGBM/CatBoost）通常仍优于或持平深度模型，且调参成本更低**。深度方法的价值更多体现在：超大规模数据、需要与文本/图像做多模态融合、或需要迁移学习/预训练时。

> 🖼️ **图片占位 7：TabNet 架构图 与 FT-Transformer 架构图**
> 资料：TabNet 论文 https://arxiv.org/abs/1908.07442 ；FT-Transformer 论文 https://arxiv.org/abs/2106.11959 ；对比研究 https://arxiv.org/abs/2207.08815

#### 2.5.1 重点模型：TabPFN —— 表格数据的「基础模型」与摊销贝叶斯推断

> 这是表格方向最具突破性的模型之一，代表「表格基础模型时刻」。下面**基于其原始论文**，侧重讲清 **原理与模型结构**（公式从略，配合结构图理解）。
>
> **原始论文**：Hollmann, Müller, Eggensperger & Hutter, *TabPFN: A Transformer That Solves Small Tabular Classification Problems in a Second*, ICLR 2023（arXiv: https://arxiv.org/abs/2207.01848 ）；升级版 TabPFN v2，*Accurate predictions on small data with a tabular foundation model*, Nature 2025（DOI: https://doi.org/10.1038/s41586-024-08328-6 ，代码 https://github.com/PriorLabs/TabPFN ）；理论基础 PFN，*Transformers Can Do Bayesian Inference*, ICLR 2022（arXiv: https://arxiv.org/abs/2112.10510 ）。

**它解决什么问题**：传统范式是「为每个数据集单独训练一个模型 + 调参 + 交叉验证」，在小数据上既慢又容易过拟合。TabPFN 把这套流程整体「摊销（amortize）」到一次离线训练里。

**核心思想（用结构而非公式理解）——先验数据拟合网络（Prior-Data Fitted Network, PFN）**：

1. **离线元训练（只做一次）**：作者先定义一个「表格数据集如何生成」的 **先验（prior）**——它基于 **结构因果模型（SCM）+ 贝叶斯神经网络**，编码了「真实表格通常因果结构简单、特征间有依赖、可能有噪声与类别不平衡」等归纳偏置（Occam's razor，偏好简单假设）。从这个先验中采样出 **数百万个合成数据集**，训练 Transformer 去预测每个合成数据集中被遮盖样本的标签。
2. **在线推理（对新数据集）**：把整个训练集 `(X_train, y_train)` 和待预测的 `X_test` **作为一个序列一次性输入** Transformer，在 **单次前向传播** 中直接输出预测——**没有梯度更新、没有超参搜索、没有交叉验证**，GPU 上约 1 秒完成。

**为什么等价于贝叶斯推断（直觉版）**：在合成先验上训练，等价于让网络学会近似「对所有可能假设按其后验加权积分」的后验预测分布。昂贵的贝叶斯积分被一次性「背」进了元训练，推理时只剩查表式前向计算。

**模型结构要点**：
- 基于 Transformer，但注意力作用于 **数据集中的样本（行）之间**，而非序列中的词。
- 训练样本之间用全注意力；测试样本只 attend 到训练样本（避免测试样本相互泄漏）。
- 在特征/样本维度做处理，以获得对特征顺序、样本数的一定不变性。

**论文结论与局限**：
- v1（ICLR 2023）：在小数据集（样本 ≤ 1000、特征 ≤ 100、类别 ≤ 10）上，**精度匹配甚至超过完整调参的 XGBoost / CatBoost / AutoML，速度快数个数量级**。
- v2（Nature 2025）：扩展到更大规模、支持回归、处理缺失值与异常值，在主流基准上 **以远低于传统方法的时间取得 SOTA**。
- 局限：对样本量/特征数有上限，超大数据集需采样或蒸馏；推理时需把训练集整体载入上下文，内存随数据规模增长。

> 🖼️ **图片占位 8：TabPFN 工作流结构图（合成先验采样 → 一次性元训练 → 单次前向 in-context 预测）**
> 资料：ICLR 2023 论文图 https://arxiv.org/abs/2207.01848 ；Nature 2025 https://doi.org/10.1038/s41586-024-08328-6 （建议直接截论文中的 workflow 示意图）

### 2.6 表格预测技术选型小结

| 场景 | 首选 | 理由 |
| --- | --- | --- |
| 快速 baseline / 强可解释（风控评分卡） | 逻辑回归 / 线性回归 | 简单、可解释、合规 |
| 中小样本、高维 | SVM / 随机森林 | 鲁棒、少调参 |
| 大多数结构化预测（默认选择） | **LightGBM / XGBoost / CatBoost** | 精度高、速度快、生态成熟 |
| 含大量高基数类别特征 | **CatBoost** | 原生类别编码最优 |
| 超大规模 + 多模态融合 + 预训练需求 | FT-Transformer / TabNet | 端到端、可迁移 |

---

## 3. 时序数据预测（Time-Series Regression & Classification）

时序预测的核心挑战：**时间依赖性、趋势（trend）、季节性（seasonality）、平稳性（stationarity）、长程依赖与多变量耦合**。技术演进主线：**统计模型 → 机器学习 → RNN → CNN/TCN → Transformer → 线性模型反击与基础模型**。

> 🖼️ **图片占位 9：时序预测技术演进时间轴**
> 推荐：ARIMA(1970s) → Prophet/GBDT(2010s) → LSTM(2014) → TCN(2018) → Informer(2021) → DLinear(2022) → PatchTST(2023) → TimesFM/基础模型(2024)。

### 3.1 经典统计模型（AR, ARIMA, ETS）

**AR / MA / ARMA / ARIMA**

- **AR(p) 自回归**：当前值由过去 p 期线性组合 + 噪声：$y_t = c + \sum_{i=1}^p \phi_i y_{t-i} + \varepsilon_t$
- **MA(q) 移动平均**：当前值由过去 q 期的误差线性组合表示。
- **ARMA = AR + MA**；**ARIMA(p,d,q)** 增加 d 阶差分（Integrated）把非平稳序列转为平稳。
- **SARIMA** 增加季节性项；**ARIMAX** 引入外生变量。

建模流程：平稳性检验（ADF 检验）→ 差分 → 用 ACF/PACF 定阶 → 拟合 → 残差白噪声检验。

**ETS（指数平滑）**：对历史观测加指数衰减权重。Holt-Winters 可同时建模 水平（level）、趋势（trend）、季节性（seasonality）三要素。

**优缺点**
- ✅ 可解释、数据需求小、单变量短序列上稳健、有置信区间。
- ❌ 假设线性与平稳，难处理多变量、长程非线性、需手工定阶。

> 🖼️ **图片占位 10：ACF/PACF 定阶图 + 时序分解（趋势/季节/残差）图**
> 资料：Hyndman《Forecasting: Principles and Practice》在线书 https://otexts.com/fpp3/

### 3.2 机器学习方法（特征工程 + GBDT）

把时序问题 **转化为监督学习的表格问题**：通过滑动窗口构造特征，再喂给 GBDT 等模型。这是工业界（尤其是需求/销量预测、Kaggle M5 竞赛）最实用的方案之一。

**关键特征工程**
- **滞后特征（Lag features）**：$y_{t-1}, y_{t-7}, y_{t-365}$
- **滑动窗口统计**：过去 7/30 天的均值、方差、最大最小、趋势斜率。
- **时间日历特征**：星期、月份、节假日、是否周末（强季节性来源）。
- **外生变量**：促销、天气、价格。

**优缺点**
- ✅ 复用成熟 GBDT 生态、可融合多源特征、可解释（特征重要性）。
- ❌ 需大量人工特征工程、不天然外推趋势（树模型无法预测超出训练范围的值）、需小心数据泄漏。

> 🖼️ **图片占位 11：滑动窗口 → 监督学习样本构造示意图**
> 资料：Kaggle M5 Forecasting 竞赛方案 https://www.kaggle.com/competitions/m5-forecasting-accuracy

### 3.3 RNN / LSTM / GRU（含 LSTM 深度解读）

**循环神经网络（RNN）** 的核心结构：用一个隐藏状态 `h_t` 沿时间步逐步传递信息——每一步把「上一步的记忆 `h_{t-1}`」和「当前输入 `x_t`」融合，再向后传。直觉上像「一边读一边记笔记」。但它有致命缺陷：训练时误差沿时间反向传播会 **指数级衰减或爆炸（梯度消失/爆炸）**，导致网络学不会相隔超过 5–10 步的依赖。

> 🖼️ **图片占位 12：RNN 按时间展开（unfold）示意图 + 梯度消失直觉图**
> 资料：Colah's blog https://colah.github.io/posts/2015-08-Understanding-LSTMs/

#### 3.3.1 重点模型：LSTM —— 用门控机制驯服长程依赖

> LSTM 是「门控 + 加性状态更新」范式的鼻祖，下面**基于原始论文**讲清 **原理与结构**（公式仅保留最关键的一条，其余用结构图理解）。
>
> **原始论文**：Hochreiter & Schmidhuber, *Long Short-Term Memory*, Neural Computation 9(8):1735–1780, 1997（PDF: https://www.bioinf.jku.at/publications/older/2604.pdf ，DOI: https://doi.org/10.1162/neco.1997.9.8.1735 ）；遗忘门补充：Gers, Schmidhuber & Cummins, *Learning to Forget*, Neural Computation 2000（https://doi.org/10.1162/089976600300015015 ）。

**它要解决的核心问题——常数误差流（Constant Error Carousel, CEC）**：标准 RNN 的记忆是「连乘」式传递，梯度必然衰减。LSTM 的关键设计是引入一条 **细胞状态（cell state）`C_t`** 作为「传送带」：它是一条**线性、自连接、权重近似恒为 1** 的通路，误差可以在其上几乎无损地长距离流动——这正是它能记住长程信息的根本原因。

**模型结构——三个门保护这条传送带**（理解结构比记公式更重要）：
- **遗忘门（forget gate）**：决定从细胞状态里 **丢弃** 哪些旧信息（2000 年补充，解决长序列下状态无界增长问题）。
- **输入门（input gate）**：决定把多少 **新信息写入** 细胞状态。
- **输出门（output gate）**：决定从细胞状态里 **读出** 多少作为当前输出 `h_t`。

把三门串起来看，细胞状态的更新本质是一条 **加性更新**：

$$C_t = f_t \odot C_{t-1} + i_t \odot \tilde{C}_t$$

正是这个「加法」（而非 RNN 的连乘）让梯度能稳定回传：当遗忘门 `f_t → 1` 时，过去的记忆近乎无损地保留下来。

**GRU** 是 LSTM 的简化版（合并为更新门、重置门），参数更少、训练更快，效果常相当。

**Seq2Seq + Attention**：编码器把历史压缩为上下文向量，解码器逐步生成未来；注意力机制让解码器关注相关历史时刻，是多步预测的常用框架，也是后来 Transformer 的前身。

**论文结论与意义**：
- 原论文在「间隔 1000 步传递信息」等合成长程任务上，证明 LSTM 能学会传统 RNN / BPTT / RTRL 全部失败的任务。
- LSTM 此后成为语音识别、机器翻译、时序预测十余年的主力，「门控 + 加性状态」思想影响深远（GRU、乃至 Transformer 的残差连接都可看作对「保护梯度通路」的延续）。
- **局限**：本质串行、难以并行训练；超长序列仍有信息瓶颈，2017 年后在多数大规模任务上被 Transformer 超越。

**优缺点小结**
- ✅ 自动学习时间依赖、可处理变长与多变量序列、长程记忆显著优于朴素 RNN。
- ❌ 串行计算慢、超长序列仍有衰减、需较多数据、易过拟合。

> 🖼️ **图片占位 13：LSTM 单元结构图（细胞状态 CEC + 遗忘/输入/输出三门）与梯度流对比图**
> 资料：原论文图；Colah's blog 经典图解 https://colah.github.io/posts/2015-08-Understanding-LSTMs/

### 3.4 CNN 与时序卷积 TCN

**思想**：用一维卷积在时间轴上滑动提取局部模式，并行度远高于 RNN。

**TCN（Temporal Convolutional Network）** 关键设计：
- **因果卷积（Causal Convolution）**：保证时刻 t 的输出只依赖 t 及之前，不泄漏未来。
- **膨胀卷积（Dilated Convolution）**：通过指数增长的膨胀因子，用较少层数获得超大感受野（覆盖长历史）。
- **残差连接**：稳定深层训练。

**WaveNet** 是其代表（最初用于音频生成），证明卷积可建模长序列。

**优缺点**
- ✅ 并行快、感受野可控、训练稳定、内存友好。
- ❌ 感受野固定（不如注意力灵活）、对超长程全局依赖弱于 Transformer。

> 🖼️ **图片占位 14：因果卷积 + 膨胀卷积感受野示意图**
> 资料：TCN 论文 https://arxiv.org/abs/1803.01271 ；WaveNet https://arxiv.org/abs/1609.03499

### 3.5 Transformer 时序家族（Informer / Autoformer / FEDformer / PatchTST）

Transformer 的自注意力可直接建模任意两个时间点的依赖，理论上完美适配长程依赖。但原始自注意力复杂度 $O(L^2)$，对长序列（L 数千）开销巨大。时序专用变体应运而生：

| 模型 | 核心创新 | 解决的问题 |
| --- | --- | --- |
| **Informer**（2021, AAAI 最佳论文） | ProbSparse 稀疏注意力，复杂度降到 $O(L\log L)$；生成式解码一次出全部预测 | 长序列效率 + 多步误差累积 |
| **Autoformer**（2021） | 序列分解（趋势+季节）内嵌进网络 + 自相关机制（Auto-Correlation）替代点对点注意力 | 显式建模季节趋势 |
| **FEDformer**（2022） | 在频域（傅里叶/小波）做注意力，复杂度 $O(L)$ | 频域捕捉全局周期 |
| **PatchTST**（2023） | 把序列切成 patch（子段）作为 token + 通道独立 | 降低 token 数、提升长程预测、强 baseline |
| **iTransformer**（2024） | 把「变量」而非「时间点」作为 token，注意力建模变量间关系 | 多变量耦合 |

**核心机制：自注意力**

$$\text{Attention}(Q,K,V)=\text{softmax}\!\left(\frac{QK^\top}{\sqrt{d_k}}\right)V$$

外加位置编码（保留时间顺序信息）。

**优缺点**
- ✅ 长程依赖建模强、并行、多变量友好、可迁移到基础模型。
- ❌ 数据需求大、计算开销高、在某些基准上未必胜过简单线性模型（见下节）。

> 🖼️ **图片占位 15：Transformer 时序家族对比图 + 自注意力热力图 + Patch 切分示意**
> 资料：Informer https://arxiv.org/abs/2012.07436 ；Autoformer https://arxiv.org/abs/2106.13008 ；FEDformer https://arxiv.org/abs/2201.12740 ；PatchTST https://arxiv.org/abs/2211.14730

### 3.6 线性模型的反击与时序基础模型（DLinear / N-BEATS / TimesFM / Chronos）

**DLinear（2022, 论文《Are Transformers Effective for Time Series Forecasting?》）**

一记响亮的「打脸」：作者用 **极简的一层线性模型 + 序列分解（趋势 + 季节分别线性映射）**，在多个长序列预测基准上 **击败了当时所有复杂 Transformer**。结论引发反思：很多时序 Transformer 的增益可能来自分解与归一化等技巧，而非注意力本身。这提醒工程师：**永远先跑简单 baseline**。

**N-BEATS / N-HiTS**：纯前馈（全连接）堆叠 + 残差「双向」分解（backcast/forecast），可解释（趋势块、季节块），在 M4 竞赛上表现优异，无需特征工程。

**时序基础模型（Foundation Models，2023+）**：借鉴大语言模型范式，在海量异构时序上预训练，支持 **零样本（zero-shot）预测**：
- **TimesFM**（Google）：decoder-only，预训练后开箱即用。
- **Lag-Llama**、**Moirai**（Salesforce）、**Chronos**（Amazon，把时序 token 化后用语言模型架构）、**TimeGPT**（Nixtla）。

这是当前最前沿方向：把「为每个任务训练一个模型」转向「一个通用模型适配所有时序任务」。

> 🖼️ **图片占位 16：DLinear 结构图 + N-BEATS 堆叠块图 + 时序基础模型零样本预测示意**
> 资料：DLinear https://arxiv.org/abs/2205.13504 ；N-BEATS https://arxiv.org/abs/1905.10437 ；Chronos https://arxiv.org/abs/2403.07815 ；TimesFM https://arxiv.org/abs/2310.10688

#### 3.6.1 重点模型：Chronos —— 把时间序列当作「一门语言」

> Chronos 代表时序基础模型里「最省事却有效」的一条路线，下面**基于原始论文**讲清 **原理与结构**（无公式，靠流程结构理解）。
>
> **原始论文**：Ansari, Stella, … Wang (Amazon), *Chronos: Learning the Language of Time Series*, TMLR 2024（arXiv: https://arxiv.org/abs/2403.07815 ，代码 https://github.com/amazon-science/chronos-forecasting ）。

**核心思想**：**几乎不改 Transformer 架构，而是把连续时间序列「翻译」成离散 token，从而直接套用成熟的语言模型（T5 家族）训练范式**。要回答的问题是——既然 LLM 能在海量文本上预训练出强大的零样本能力，时序能否照搬？

**模型结构——三步「时序 → 语言」的 tokenization**（这是论文关键贡献，按流程理解即可）：
1. **缩放（Scaling）**：用 mean scaling 把序列归一化（除以历史绝对值均值），消除不同序列的量纲差异。
2. **量化（Quantization）**：把归一化后的连续值用分箱（binning）映射到一个 **固定大小的离散词表**（如 4096 个 token），每个 bin 就是一个「词」。这一步把回归问题变成了分类问题。
3. **语言模型训练**：在这些 token 序列上，用标准 **交叉熵损失** 训练 T5（encoder-decoder）或 GPT 风格模型 **预测下一个 token**——与训练语言模型完全一致。预测因此天然是一个 token 上的概率分布，**直接支持概率预测**（采样多条轨迹得到预测区间）。

**数据策略**：在大量公开真实时序上预训练，并用两种数据增强扩充多样性——**TSMix**（混合多条序列）和 **KernelSynth**（用高斯过程核合成全新序列），缓解时序公开数据稀缺的问题。

**论文结论与局限**：
- 在 **从未见过的数据集上做零样本（zero-shot）预测**，效果可与在这些数据集上专门训练的模型相当甚至更好；分布内任务上超过多数专用基线。
- 证明了「语言建模范式 + 离散化 token」是构建时序基础模型的一条简洁可行路径。
- 局限：量化带来精度上限（bin 粒度限制数值分辨率）；上下文长度受限于底层 LM；极端尺度/强外生变量场景仍需适配（后续有 **Chronos-Bolt** 等加速版本）。

> **与 TabPFN 的范式呼应**：两者都是「基础模型 + 零样本」思路，但路线不同——TabPFN 用 **合成先验做摊销贝叶斯推断**，Chronos 用 **真实数据 + 语言模型 token 化**。这代表 2023–2025 年预测建模最重要的趋势：**从「一任务一模型」走向「预训练通用模型 + 零样本/少样本适配」**。

> 🖼️ **图片占位 17：Chronos 的 scaling → quantization → 语言模型 token 预测 流程图**
> 资料：论文图 https://arxiv.org/abs/2403.07815

### 3.7 时序分类专题

时序分类（Time-Series Classification, TSC）的目标是给整段序列打标签（如 ECG 是否异常、设备是否将故障、人体活动识别）。主流技术：

| 类别 | 代表方法 | 原理 |
| --- | --- | --- |
| **距离 + 最近邻** | DTW + 1-NN | 动态时间规整对齐两序列后算距离，经典强 baseline |
| **特征/字典法** | BOSS、Shapelet、Catch22 | 提取判别性子序列（shapelet）或符号化词袋 |
| **集成法** | HIVE-COTE 2.0 | 多种表征的大集成，长期 SOTA（但慢） |
| **卷积核** | ROCKET / MiniROCKET | 大量随机卷积核 + 线性分类器，极快且精度高 |
| **深度学习** | InceptionTime、TCN、Transformer | 端到端表征学习，大数据上强 |

**实践建议**：小数据先试 ROCKET/MiniROCKET（性价比之王）或 DTW+1NN baseline；大数据用 InceptionTime 等深度模型。工具库推荐 `sktime`、`tslearn`、`tsai`。

> 🖼️ **图片占位 18：DTW 对齐示意图 + ROCKET 随机卷积核流程图 + Shapelet 示意**
> 资料：ROCKET https://arxiv.org/abs/1910.13051 ；InceptionTime https://arxiv.org/abs/1909.04939 ；UCR/UEA 时序分类基准 https://www.timeseriesclassification.com/

---

## 4. 横向对比与工程实践建议

### 4.1 三个「重点深读」模型横向对比（LSTM / TabPFN / Chronos）

> 这三个模型分别在 §3.3、§2.5、§3.6 做了深度解读，分别代表「时序经典深度学习 / 表格基础模型 / 时序基础模型」三个方向。下表做统一对比，方便回顾。

| 维度 | LSTM (1997/2000) | TabPFN (2023/2025) | Chronos (2024) |
| --- | --- | --- | --- |
| 数据类型 | 时序（序列） | 表格 | 时序 |
| 任务 | 回归/分类/生成 | 分类（v1）/+回归（v2） | 概率回归预测 |
| 核心机制 | 门控 + CEC 加性状态 | 合成先验上的摊销贝叶斯推断 | 时序离散化 + 语言模型 |
| 训练范式 | 每任务单独训练 | 一次元训练，推理零梯度 | 大规模预训练 |
| 零样本能力 | ❌ | ✅（in-context） | ✅ |
| 关键优势 | 长程依赖、经典稳健 | 小数据秒级 SOTA、免调参 | 跨域零样本、概率预测 |
| 主要局限 | 串行、难并行、长序列瓶颈 | 数据规模上限、上下文内存 | 量化精度、上下文长度 |
| 论文链接 | [1997](https://doi.org/10.1162/neco.1997.9.8.1735) / [2000](https://doi.org/10.1162/089976600300015015) | [ICLR'23](https://arxiv.org/abs/2207.01848) / [Nature'25](https://doi.org/10.1038/s41586-024-08328-6) | [arXiv:2403.07815](https://arxiv.org/abs/2403.07815) |

### 4.2 模型能力总览

| 模型族 | 数据类型 | 任务 | 数据量需求 | 可解释性 | 训练成本 | 典型场景 |
| --- | --- | --- | --- | --- | --- | --- |
| 线性/逻辑回归 | 表格 | 回归/分类 | 小 | ★★★★★ | 极低 | 风控评分卡、baseline |
| SVM | 表格 | 回归/分类 | 小-中 | ★★ | 中 | 高维中小样本 |
| 随机森林 | 表格 | 回归/分类 | 中 | ★★★ | 低 | 稳健 baseline |
| **GBDT 三巨头** | 表格 | 回归/分类 | 中-大 | ★★★ | 中 | **结构化预测默认选择** |
| 表格 DL | 表格 | 回归/分类 | 大 | ★★ | 高 | 多模态/预训练 |
| ARIMA/ETS | 时序 | 回归 | 小 | ★★★★ | 低 | 单变量短序列、可解释 |
| GBDT+特征工程 | 时序 | 回归 | 中-大 | ★★★ | 中 | 需求/销量预测 |
| LSTM/GRU | 时序 | 回归/分类 | 中-大 | ★★ | 高 | 多变量、非线性 |
| TCN | 时序 | 回归/分类 | 中-大 | ★★ | 中 | 长序列、需并行 |
| Transformer 家族 | 时序 | 回归 | 大 | ★★ | 高 | 长程多变量预测 |
| DLinear/N-BEATS | 时序 | 回归 | 中 | ★★★ | 低 | 强 baseline |
| 时序基础模型 | 时序 | 回归 | 预训练 | ★★ | 极高(预训练) | 零样本/通用预测 |
| ROCKET/InceptionTime | 时序 | 分类 | 中 | ★★ | 低-中 | 时序分类 SOTA |

### 4.3 选型决策原则

1. **先 baseline 再复杂**：表格先跑 LightGBM，时序先跑 ARIMA/DLinear/GBDT+特征。复杂模型必须证明能显著超过 baseline 才值得上线。
2. **数据量决定上限**：深度模型吃数据；小数据下传统方法往往更稳。
3. **可解释性与合规**：金融、医疗强监管场景优先可解释模型（线性、树）。
4. **评估指标对齐业务**：回归用 MAE/RMSE/MAPE/SMAPE；分类用 AUC/F1/PR-AUC（注意类别不平衡）；时序需用 **时间序列交叉验证（滚动/扩展窗口）**，严禁随机打乱导致未来信息泄漏。
5. **防数据泄漏**：时序特征、目标编码、标准化都必须在「训练时间窗内」拟合。

> 🖼️ **图片占位 19：模型选型决策流程图（Decision Tree 形式）**
> 推荐自制：以「数据类型？→ 数据量？→ 可解释要求？→ 是否多变量长程？」为分支的流程图。

> 🖼️ **图片占位 20：时间序列交叉验证（滚动/扩展窗口）示意图**
> 资料：scikit-learn TimeSeriesSplit https://scikit-learn.org/stable/modules/cross_validation.html#time-series-split

---

## 5. 参考资料与配图清单

### 5.1 经典论文与资料链接

**重点深读模型（分别见 §2.5 TabPFN、§3.3 LSTM、§3.6 Chronos）**
- LSTM（原始）: Hochreiter & Schmidhuber, Neural Computation 1997 https://doi.org/10.1162/neco.1997.9.8.1735 （PDF: https://www.bioinf.jku.at/publications/older/2604.pdf ）
- LSTM 遗忘门: Gers, Schmidhuber & Cummins, Neural Computation 2000 https://doi.org/10.1162/089976600300015015
- TabPFN（ICLR 2023）: https://arxiv.org/abs/2207.01848 ；TabPFN v2（Nature 2025）: https://doi.org/10.1038/s41586-024-08328-6 ；代码: https://github.com/PriorLabs/TabPFN
- PFN 理论（Transformers Can Do Bayesian Inference, ICLR 2022）: https://arxiv.org/abs/2112.10510
- Chronos（TMLR 2024）: https://arxiv.org/abs/2403.07815 ；代码: https://github.com/amazon-science/chronos-forecasting

**表格 / 集成学习**
- XGBoost: https://arxiv.org/abs/1603.02754
- LightGBM: https://proceedings.neurips.cc/paper/2017/file/6449f44a102fde848669bdd9eb6b76fa-Paper.pdf
- CatBoost: https://arxiv.org/abs/1706.09516
- TabNet: https://arxiv.org/abs/1908.07442
- FT-Transformer: https://arxiv.org/abs/2106.11959
- 「深度学习并非表格数据全部」: https://arxiv.org/abs/2106.03253 ；Grinsztajn 2022: https://arxiv.org/abs/2207.08815

**时序**
- Forecasting: Principles and Practice（在线书）: https://otexts.com/fpp3/
- Informer: https://arxiv.org/abs/2012.07436
- Autoformer: https://arxiv.org/abs/2106.13008
- FEDformer: https://arxiv.org/abs/2201.12740
- PatchTST: https://arxiv.org/abs/2211.14730
- iTransformer: https://arxiv.org/abs/2310.06625
- DLinear（Transformers 是否有效）: https://arxiv.org/abs/2205.13504
- N-BEATS: https://arxiv.org/abs/1905.10437 ；N-HiTS: https://arxiv.org/abs/2201.12886
- TCN: https://arxiv.org/abs/1803.01271
- Chronos: https://arxiv.org/abs/2403.07815 ；TimesFM: https://arxiv.org/abs/2310.10688

**时序分类**
- ROCKET: https://arxiv.org/abs/1910.13051 ；MiniROCKET: https://arxiv.org/abs/2012.08791
- InceptionTime: https://arxiv.org/abs/1909.04939
- UCR/UEA 基准: https://www.timeseriesclassification.com/

**常用工具库**
- scikit-learn, XGBoost, LightGBM, CatBoost
- 时序：statsmodels, Prophet, Darts, GluonTS, Nixtla(neuralforecast/statsforecast), sktime, tsai

### 5.2 配图获取清单（共 20 处占位）

| 编号 | 配图主题 | 获取方式建议 |
| --- | --- | --- |
| 1 | 预测任务技术地图（四象限+时间轴） | 自制 |
| 2 | 表格模型技术演进树 | 自制 |
| 3 | 逻辑回归 Sigmoid 决策边界 | scikit-learn 文档截图 |
| 4 | SVM 最大间隔与支持向量 | scikit-learn 文档截图 |
| 5 | Bagging vs Boosting / GBDT 残差 | StatQuest 视频截图 |
| 6 | Level-wise vs Leaf-wise + 直方图 | LightGBM 文档截图 |
| 7 | TabNet / FT-Transformer 架构 | 对应论文截图 |
| 8 | TabPFN 工作流（合成先验→元训练→in-context 预测） | ICLR'23/Nature'25 论文截图 |
| 9 | 时序技术演进时间轴 | 自制 |
| 10 | ACF/PACF + 时序分解 | fpp3 在线书截图 |
| 11 | 滑动窗口构造监督样本 | 自制 / Kaggle M5 方案图 |
| 12 | RNN 按时间展开 + 梯度消失直觉 | Colah's blog 截图 |
| 13 | LSTM 单元结构（CEC+三门）与梯度流 | Colah's blog / 原论文截图 |
| 14 | 因果卷积 + 膨胀卷积感受野 | TCN/WaveNet 论文截图 |
| 15 | Transformer 时序家族 + 注意力热力图 | 各论文截图 |
| 16 | DLinear / N-BEATS / 基础模型零样本 | 对应论文截图 |
| 17 | Chronos scaling→quantization→LM token 预测流程 | arXiv:2403.07815 论文截图 |
| 18 | DTW / ROCKET / Shapelet | 对应论文截图 |
| 19 | 模型选型决策流程图 | 自制 |
| 20 | 时间序列交叉验证示意 | scikit-learn 文档截图 |

---

## 附录 A. 可参考的同类开源项目推荐

> 下列项目可作为本报告的「代码落地参照」与「配图素材来源」。⭐ 数为 2026-06 量级，按用途分组，并标注与本报告对应的章节。

### A.1 统一框架 / 工程落地（开箱即用）

| 项目 | ⭐ | 说明 | 对应章节 |
| --- | --- | --- | --- |
| [sktime/sktime](https://github.com/sktime/sktime) | ~9.8k | 统一的时序机器学习框架，覆盖预测 / 分类 / 回归，API 类 scikit-learn | §3、§3.7 |
| [unit8co/darts](https://github.com/unit8co/darts) | ~9.4k | 最易用的时序预测库，统一封装 ARIMA→Transformer→基础模型，自带回测 | §3 全章 |
| [Nixtla/neuralforecast](https://github.com/Nixtla/neuralforecast) | ~4.2k | 可扩展神经预测库，含 N-BEATS / N-HiTS / TFT / PatchTST 等 | §3.5、§3.6 |

> 同类常用库（生态成熟、可一并参考）：`awslabs/gluonts`（概率预测）、`Nixtla/statsforecast`（高性能统计模型）。

### A.2 深度时序模型库 / 基准（最契合本报告主线）

| 项目 | ⭐ | 说明 | 对应章节 |
| --- | --- | --- | --- |
| [thuml/Time-Series-Library](https://github.com/thuml/Time-Series-Library) | ~12.5k | 先进深度时序模型的统一实现与基准（Informer/Autoformer/FEDformer/PatchTST/iTransformer/DLinear/TimesNet 等），**强烈推荐对照 §3.4–§3.6** | §3.4–§3.6 |
| [qingsongedu/time-series-transformers-review](https://github.com/qingsongedu/time-series-transformers-review) | ~3.0k | Transformers in Time Series 论文/代码精选清单 | §3.5 |
| [qingsongedu/awesome-AI-for-time-series-papers](https://github.com/qingsongedu/awesome-AI-for-time-series-papers) | ~1.6k | 顶会顶刊 AI×时序论文/教程/综述清单 | §3、§5 |

### A.3 表格深度学习（对应 §2.5）

| 项目 | ⭐ | 说明 |
| --- | --- | --- |
| [yandex-research/rtdl](https://github.com/yandex-research/rtdl) | ~1.1k | 表格深度学习研究合集（FT-Transformer 等的官方实现入口） |
| [yandex-research/rtdl-revisiting-models](https://github.com/yandex-research/rtdl-revisiting-models) | ~347 | NeurIPS'21 *Revisiting DL Models for Tabular Data*，FT-Transformer 出处 |
| [pyg-team/pytorch-frame](https://github.com/pyg-team/pytorch-frame) | ~788 | PyTorch 表格深度学习库（统一封装多种表格模型） |
| [DataCanvasIO/DeepTables](https://github.com/DataCanvasIO/DeepTables) | ~700 | 面向结构化数据的深度学习工具箱（DeepFM/Wide&Deep 等） |

### A.4 基础模型官方实现（对应 §2.5 TabPFN、§3.6 Chronos / TimesFM）

| 项目 | ⭐ | 说明 |
| --- | --- | --- |
| [google-research/timesfm](https://github.com/google-research/timesfm) | ~21k | Google 的时序基础模型 TimesFM 官方实现 |
| [PriorLabs/TabPFN](https://github.com/PriorLabs/TabPFN) | ~7.4k | 表格基础模型 TabPFN 官方实现（v2） |
| [amazon-science/chronos-forecasting](https://github.com/amazon-science/chronos-forecasting) | ~5.5k | Amazon 的 Chronos 时序基础模型官方实现 |

### A.5 资源清单（Awesome 系列，找配图/扩展阅读）

| 项目 | ⭐ | 说明 |
| --- | --- | --- |
| [Alro10/deep-learning-time-series](https://github.com/Alro10/deep-learning-time-series) | ~2.8k | 深度学习时序预测论文/代码/实验清单 |
| [MaxBenChrist/awesome_time_series_in_python](https://github.com/MaxBenChrist/awesome_time_series_in_python) | ~2.3k | Python 时序分析包清单 |
| [cuge1995/awesome-time-series](https://github.com/cuge1995/awesome-time-series) | ~1.1k | 时序论文/代码/资源清单 |
| [rob-med/awesome-TS-anomaly-detection](https://github.com/rob-med/awesome-TS-anomaly-detection) | ~3.2k | 时序异常检测工具与数据集清单（可作分类/检测扩展） |

> **怎么用**：要快速复现报告里的模型对照实验，首选 `thuml/Time-Series-Library`（时序）与 `yandex-research/rtdl` / `pyg-team/pytorch-frame`（表格）；要做工程落地与回测，用 `darts` / `sktime` / `neuralforecast`；要体验「零样本基础模型」，直接装 `timesfm` / `TabPFN` / `chronos-forecasting`。

---

> **一句话总结**：表格数据，**GBDT（XGBoost/LightGBM/CatBoost）仍是王者**；时序数据，**从 ARIMA 到 Transformer 再到基础模型，但务必先用 DLinear / GBDT+特征工程 等简单 baseline 验证收益**。技术选型的第一原则永远是：**先简单，后复杂；先 baseline，后调优**。
